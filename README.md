# php with roadrunner in docker

install docker, docker-compose

to run:
 - docker-compose up -d --build
 - docker-compose run roadrunner sh -c 'composer install'
 - see http://localhost:8080/

to down:
 - docker-compose down
